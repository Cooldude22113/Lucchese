Current Status

Lucchese is NOT yet production mature.

Observability Progress:
  STAB-001 deployed — main chat and voice paths are observable
  context_assembly log entries produced on every main-path inference
  tier failures now surface as WARNING entries before user-visible degradation
  total_assembled_char_count provides first runtime prompt size measurement
  STAB-003 deployed — RotatingFileHandler active on lucchese.context stream
    maxBytes=5_242_880 (5 MB), backupCount=5, total cap ~25 MB
    log_path confirmed in log_rotation_initialized startup record
    two open follow-ups tracked: STAB-003-FU-01 (relative path), STAB-003-FU-02 (OSError guard)
  STAB-005 deployed — startup validation active on every process start
    hard-stop enforced on critical dependency failures
    environment, SQLite connectivity, table presence, schema integrity,
    profile_state row presence, and ChromaDB connectivity checked in sequence
    lucchese.startup logger active on console
    AD-013 satisfied

Remaining Production Blockers:
  profile_state unpopulated — tier1_status: "empty_source" on all inferences
    now visible at startup (STAB-005) and at inference time (STAB-001)
  intercept paths (chat.py action plan, scrape) produce no observability entries
  lucchese.startup logger has no file handler — startup failures leave no disk
    trace if process manager does not capture stdout (TD-004, tracked)
  schema drift between ingestion and reclassification (AD-016)
  no token budget enforcement (AD-015)
  no automated test coverage for observability behavior

Tracked Technical Debt (non-blocking):
  TD-001 — _PassthroughJsonFormatter duplicated across startup_validator.py and main.py
    resolve before any log parsing tooling is built
  TD-002 — check_required_tables and check_state_schema open read-write connections
    inconsistent with module read-only contract; batch with TD-003
  TD-003 — check_state_schema conn.close() not in a finally block
    connection leak risk on uncaught exception; batch with TD-002
  TD-004 — lucchese.startup logger has no file handler
    resolve before any unattended deployment scenario; resolve TD-001 first

Current Production Blockers (updated):
  inconsistent observability coverage (intercept paths)
  incomplete rollback discipline
  partial stabilization of memory systems
  incomplete deployment procedures
  limited automated testing

Deployment Prerequisites:
  Confirm lucchese.context stream is visible to monitoring before go-live
  Brief monitoring personnel: tier1_status: "empty_source" is expected initial state
  Verify startup_validation_complete appears in console on first start
  Verify overall_status is "ok" or "degraded" — "failed" means server did not start
  Verify one manual inference produces correct log output before enabling traffic
  Brief monitoring personnel: not_seeded warning at startup is expected until STAB-002

Operational Philosophy (unchanged):
  The system should fail visibly, explicitly, recoverably.
  not silently, partially, unpredictably.
  STAB-001 advances this for the main inference paths.
  STAB-005 advances this for startup and dependency failures.
  Remaining gap: intercept paths (chat.py action plan, scrape) still unobserved.